{
  "version": 2,
  "builds": [
    { "src": "api/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/cron", "dest": "api/cron.ts" },
    { "src": "/(.*)", "dest": "api/index.ts" }
  ],
  "crons": [
    { "path": "/api/cron", "schedule": "30 6 * * *" }
  ]
}
